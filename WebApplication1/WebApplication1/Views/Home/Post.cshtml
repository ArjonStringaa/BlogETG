@model Post
@{


 }

@*<h2 class = "post">@Model.Title</h2>

    @if (!String.IsNullOrEmpty(Model.Image))
    {
    var image_path = $"/Image/{Model.Image}";
    <img src="@image_path" width="500"/>
    }
<p>@Model.Body</p>*@

<div class="container">
<div class="post">
    <h2 class = "post">@Model.Title</h2>

    @if (!String.IsNullOrEmpty(Model.Image))
    {
    var image_path = $"/Image/{Model.Image}";
    <img src="@image_path" />
    <span>@Model.Title</span>
    }
</div>
<div class="post-body">
    @Model.Body;
</div>

</div>


<p style="color:white; font-size:30px;">HERE</p>